{
  "name": "fides-api-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "FIDES Community API gateway — single hostname proxying to per-catalog Vercel backends",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.2"
  }
}
